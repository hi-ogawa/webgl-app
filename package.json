{
  "type": "module",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "rm -r dist && parcel build src/index.html",
    "deploy": "now --debug -A config/now.json",
    "build:deploy": "npm run build && npm run deploy"
  },
  "devDependencies": {
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.1",
    "mocha": "^7.1.2",
    "now": "^19.0.0",
    "parcel-bundler": "^1.12.4",
    "serve": "^11.3.0",
    "snowpack": "^2.0.0-beta.16"
  },
  "dependencies": {
    "lodash": "^4.17.15",
    "three": "^0.116.1"
  },
  "browserslist": [
    "last 1 Chrome versions"
  ]
}
